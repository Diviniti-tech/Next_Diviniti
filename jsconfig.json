{
  "compilerOptions": {
    "baseUrl": ".",  // Définit la racine du projet pour les chemins relatifs
    "paths": {
      "@/*": ["./src/*"]  // Définit l'alias `@` pour le dossier `src`
    }
  }
}
